export default () => {
    return (
        <div>
            <h1>Navigation</h1>
            <p>Navigation component is used to create a fixed navigation bar on the side of the page. It can be used to create a sidebar or a top navigation bar.</p>
            <p>It has two main components: Navigation and Navigation.Content. Navigation is the main container for the navigation bar, while Navigation.Content is used to wrap the content that will be displayed inside the navigation bar.</p>
            <p>Navigation component accepts an offsetTop prop which is used to set the distance from the top of the page. This is useful when you have a fixed header and you want to make sure that the navigation bar does not overlap with it.</p>
        </div>
    );
}